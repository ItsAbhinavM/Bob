apiVersion: v1
kind: Service

metadata:
  name: bob-frontend-service

spec:
  selector:
    app: bob-frontend

  ports:
  - protocol: TCP
    port: 80
    targetPort: 3001
    nodePort: 30001

  type: NodePort
